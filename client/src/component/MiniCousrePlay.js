import React, {Fragment, useState, useEffect} from 'react';
import {Player} from 'video-react';


const MiniCoursePlay = (props) => {
    useEffect(() => {
        console.log("play : ", props);
    },[]);
    return(
        <Fragment>
            hello
        </Fragment>
    );
};

export default MiniCoursePlay;