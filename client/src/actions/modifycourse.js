import {MODIFY_COURSE} from "./types";
import axios from 'axios';

export const modifyCourse = () => async dispatch => {
    const config = {
        headers: {
            'Content-Type': 'application/json'
        }
    };
    try {

    } catch (e) {

    }

};